import React, { useState } from "react";

const Counter = () => {
  return React.createElement(
    "div",
    { id: "hello", className: "text-red-500" },
    React.createElement("b", null, "Hello Shibili cv")
  );
};

export default Counter;